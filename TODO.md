# Fix Auth Login Pydantic Error - Secure Refactor to AuthService\n\n## Status: [ ] 0% Complete\n\n1. [ ] Update auth.py imports: Add AuthService from app.services.services, remove direct User/DB/bcrypt/jwt.\n2. [ ] Refactor login(): Use svc = AuthService(db); token_resp, raw_rt = svc.login(body); set cookie; return token_resp.\n3. [ ] Refactor refresh_token(): Use svc.refresh(raw_rt); update cookie; return token_resp.\n4. [ ] Update logout(): Use svc.logout(user_id); delete cookie.\n5. [ ] Test: Restart server, POST /api/v1/auth/login, verify expires_in present + no error.\n6. [ ] [ ] Verify secure: Check refresh rotation revokes old token, logout revokes all RTs.\n\n**Notes:** Uses DB refresh token storage for revocation security. Dynamic expires_in from settings.\n
